import 'package:flutter/material.dart';

class EditScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('予定の編集')),
    );
  }
}
