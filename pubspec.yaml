name: flutter_calendar
description: flutter calendar application

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  intl: ^0.16.1
  modal_progress_hud: ^0.1.3
  firebase_auth: ^0.18.0+1
  firebase_core: ^0.5.0
  flutter_calendar_carousel: ^1.4.12


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

